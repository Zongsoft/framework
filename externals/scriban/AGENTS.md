## 概述

本目录遵循 [../AGENTS.md](../AGENTS.md)。本项目通过 Scriban 提供表达式计算和文本模板渲染。

## 工作边界

- 保持模板变量、成员访问、函数注册、文化区域和错误定位语义稳定。
- 未受信模板的可调用对象必须受控，不无意暴露宿主服务或敏感成员。
- 渲染上下文应隔离，不在并发请求间复用可变状态。
- 不记录完整模板数据或敏感渲染结果。

## 验证

- 构建 `Zongsoft.Externals.Scriban.slnx`。
- 验证空值、循环/条件、自定义函数、转义、语法错误和并发上下文隔离。
