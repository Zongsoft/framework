# 序列示例

[English](README.md) | [简体中文](README.zh-Hans.md)

本示例在 `samples:sequence` 命名空间中分别递增一个整数序列和一个浮点数序列。

```shell
dotnet run --project Zongsoft.Externals.Etcd.Sequence.Samples.csproj
```

可通过第一个命令行参数传入 etcd 连接字符串；默认值为 `server=127.0.0.1;port=2379`。
